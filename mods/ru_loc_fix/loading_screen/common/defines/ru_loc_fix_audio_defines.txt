# ru_loc_fix: the game asks Wwise for construction_building_<category>_<tier>_finished
# when a building completes, and ships none of those events: every category and
# tier in the 09-26 logs is "not found". Vanilla is 2 (click and completion);
# 0 keeps the click sound, which is not missing, and drops the request for a
# sound that does not exist.
NCityAudio = {
	CONSTRUCTION_AUDIO_MODE = 0
}

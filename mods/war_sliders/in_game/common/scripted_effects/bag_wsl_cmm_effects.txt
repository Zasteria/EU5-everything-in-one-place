# Страница мода в меню CMF. Читается кнопкой CMF в игре.
#
# **Макрос CMM, позванный с аргументом, которого CMF не объявляет, молча не
# делает ничего и уносит с собой весь остаток эффекта.** Проверять
# `python3 tools/check_cmm.py mods/war_sliders/in_game/common` после любой правки
# здесь.

# Scope: country
bag_wsl_register = {
	# Содержание (maintenance) Group
	cmm_register_bool_setting = {
		mod_id = bag_wsl
		setting_id = army_enabled
		tab_id = main
		group_id = maintenance
		default_value = 1
	}
	cmm_sync_bool_alias = {
		setting = bag_wsl__army_enabled
		alias = bag_wsl_army_on
	}

	cmm_register_slider_setting = {
		mod_id = bag_wsl
		setting_id = army_target
		tab_id = main
		group_id = maintenance
		default_value = 50
		min_value = 50
		max_value = 100
		step_value = 5
	}
	cmm_sync_setting_alias = {
		setting = bag_wsl__army_target
		alias = bag_wsl_army_pct
	}

	cmm_register_bool_setting = {
		mod_id = bag_wsl
		setting_id = navy_enabled
		tab_id = main
		group_id = maintenance
		default_value = 1
	}
	cmm_sync_bool_alias = {
		setting = bag_wsl__navy_enabled
		alias = bag_wsl_navy_on
	}

	cmm_register_slider_setting = {
		mod_id = bag_wsl
		setting_id = navy_target
		tab_id = main
		group_id = maintenance
		default_value = 50
		min_value = 50
		max_value = 100
		step_value = 5
	}
	cmm_sync_setting_alias = {
		setting = bag_wsl__navy_target
		alias = bag_wsl_navy_pct
	}

	cmm_register_bool_setting = {
		mod_id = bag_wsl
		setting_id = fort_enabled
		tab_id = main
		group_id = maintenance
		default_value = 1
	}
	cmm_sync_bool_alias = {
		setting = bag_wsl__fort_enabled
		alias = bag_wsl_fort_on
	}

	cmm_register_slider_setting = {
		mod_id = bag_wsl
		setting_id = fort_target
		tab_id = main
		group_id = maintenance
		default_value = 50
		min_value = 50
		max_value = 100
		step_value = 5
	}
	cmm_sync_setting_alias = {
		setting = bag_wsl__fort_target
		alias = bag_wsl_fort_pct
	}

	# Чеканка (minting) Group
	cmm_register_bool_setting = {
		mod_id = bag_wsl
		setting_id = minting_enabled
		tab_id = main
		group_id = minting
		default_value = 1
	}
	cmm_sync_bool_alias = {
		setting = bag_wsl__minting_enabled
		alias = bag_wsl_mint_on
	}

	cmm_register_slider_setting = {
		mod_id = bag_wsl
		setting_id = minting_target
		tab_id = main
		group_id = minting
		default_value = 0
		min_value = 0
		max_value = 100
		step_value = 5
	}
	cmm_sync_setting_alias = {
		setting = bag_wsl__minting_target
		alias = bag_wsl_mint_pct
	}

	cmm_register_slider_setting = {
		mod_id = bag_wsl
		setting_id = inflation_target
		tab_id = main
		group_id = minting
		default_value = 0
		min_value = 0
		max_value = 50
		step_value = 1
	}
	cmm_sync_setting_alias = {
		setting = bag_wsl__inflation_target
		alias = bag_wsl_infl_pct
	}

	# Диагностика (debug) Group
	cmm_register_bool_setting = {
		mod_id = bag_wsl
		setting_id = show_lab
		tab_id = main
		group_id = debug
		default_value = 0
	}
	cmm_sync_bool_alias = {
		setting = bag_wsl__show_lab
		alias = bag_wsl_lab_on
	}
}

# Обратный звонок CMF: `var:cmf_callback` — флаг того, что тронули.
# Scope: country
bag_wsl_handle_callback = {
	switch = {
		trigger = var:cmf_callback
		flag:bag_wsl__army_enabled = {
			cmm_sync_bool_alias = {
				setting = bag_wsl__army_enabled
				alias = bag_wsl_army_on
			}
		}
		flag:bag_wsl__army_target = {
			cmm_sync_setting_alias = {
				setting = bag_wsl__army_target
				alias = bag_wsl_army_pct
			}
		}
		flag:bag_wsl__navy_enabled = {
			cmm_sync_bool_alias = {
				setting = bag_wsl__navy_enabled
				alias = bag_wsl_navy_on
			}
		}
		flag:bag_wsl__navy_target = {
			cmm_sync_setting_alias = {
				setting = bag_wsl__navy_target
				alias = bag_wsl_navy_pct
			}
		}
		flag:bag_wsl__fort_enabled = {
			cmm_sync_bool_alias = {
				setting = bag_wsl__fort_enabled
				alias = bag_wsl_fort_on
			}
		}
		flag:bag_wsl__fort_target = {
			cmm_sync_setting_alias = {
				setting = bag_wsl__fort_target
				alias = bag_wsl_fort_pct
			}
		}
		flag:bag_wsl__minting_enabled = {
			cmm_sync_bool_alias = {
				setting = bag_wsl__minting_enabled
				alias = bag_wsl_mint_on
			}
		}
		flag:bag_wsl__minting_target = {
			cmm_sync_setting_alias = {
				setting = bag_wsl__minting_target
				alias = bag_wsl_mint_pct
			}
		}
		flag:bag_wsl__inflation_target = {
			cmm_sync_setting_alias = {
				setting = bag_wsl__inflation_target
				alias = bag_wsl_infl_pct
			}
		}
		flag:bag_wsl__show_lab = {
			cmm_sync_bool_alias = {
				setting = bag_wsl__show_lab
				alias = bag_wsl_lab_on
			}
		}
	}
}

# Список владельца после игры, 2026-09-12 — и что в нём измерено

**Это тот самый список, которого не было в документах**
([`NEXT_SESSION.md`](../NEXT_SESSION.md) просил его спросить). Он пришёл после
партии одним сообщением: девять пунктов плюс решение, которое их все окрашивает.
Здесь он записан целиком, каждый пункт — его словами, а под ним то, что **уже
измерено по коду**, и что для него надо построить.

**Ничего из этого в игре не проверено.** Пометка «измерено» значит «прочитано в
файлах игры и посчитано скриптом», не «видено в партии».

---

## Решение, из которого следует половина работы: CM и Glorp UI убраны

«Я решил полностью отказаться от CM и GlorpUI… Они оба снижают
производительность игры. Особенно сильно это делает CM. Поэтому переключатель
нашей автоматизации должен работать теперь не через CM, а через ванильную
систему автоматизации строительства.»

**CMF остаётся** — страница мода на нём, и он про него говорит отдельно
(«отдельную кнопку CMF»). Уходит `romaimperator.construction_manager`.

**Что мод берёт у CM сейчас** (измерено, `grep` по дереву мода): ровно две вещи.

| что | имя CM | где у нас |
| --- | --- | --- |
| галочки автостроя | `cm_auto_expand_registered_building_types`, `cm_auto_expand_excluded_building_types`, `cm_mass_auto_expand_building_types` | `bag_wtp_cm_apply` в `bag_wtp_generated_plan.txt` |
| пометка «житница» | `cm_auto_food_location_enabled` | `bag_wtp_is_granary` в `bag_wtp_generated_triggers.txt` |

**Ванильная замена автостроя — только из интерфейса, и это измерено.** В дампах
игры есть `IsAutoExpand( Arg0 )`, `ToggleAutoExpandBuilding( Arg0 )`,
`ToggleAutoExpandBuildings( Arg0 )`, `AreBuildingsAutoExpand( Arg0 )` и
`BuildInLocationLateralView.ToggleAutoExpandAllBuildings` — **всё это функции
GUI**. Ни эффекта, ни триггера с этим смыслом в дампах нет: `api.py --find
auto_expand` не находит ничего, кроме чужого `on_action` CM. Значит **отмашка
скриптом невозможна**, и наша кнопка обязана стать виджетом, который зовёт
`ToggleAutoExpandBuilding` на здании.

**И отсюда его единственная добавка**: «чтобы ещё не построенные здания всё так
же имели возможность включить для себя автоматизацию». Ванильная галочка висит
на `Building.Self` — на **существующем** здании; у непостроенного её нет.
Значит это наше: помнить намерение на локации и отдать его игре, когда здание
появится, либо строить первый уровень самим.

---

## Что осталось из первых девяти

Первые три захода закрыты и лежат в
[`../archive/wtp_backlog_third_pass.md`](../archive/wtp_backlog_third_pass.md)
— там же девять пунктов его первого прихода целиком. Открыто из них два:

- **9. Режим ручного заполнения.** «Выбрал землю, включил нужные тумблеры села,
  города, житницы. И заполняешь самостоятельно каждую локацию тем, что тебе
  нужно… Далее сделал, настроил, включил автостроительство.» Половина этого —
  окно замены, построенное 2026-09-13; не хватает тумблеров и отмашки.
- **Ванильная отмашка автостроя.** CM снят, а замена ей живёт только в
  интерфейсе: `ToggleAutoExpandBuilding` на существующем здании. Для
  непостроенного придётся помнить намерение на локации.

## Автострой: его идея работает, и вот чем

Его вопрос, 2026-09-14: «Почему мы не можем подсунуть игре сигнал, что здание
там есть… И тогда игра будет просто смотреть, что здание не достигло своего
лимита, даже если там 0/1, и будет стремиться расширить его до этого лимита.»

**Подсовывать ничего не надо -- здание можно просто поставить.**
`change_building_level_in_location = {{ building = <тип> value = <на сколько>
{{ owner = <держава> }} }}` -- эффект в скоупе локации, есть в дампах игры.
Здание после него **существует**, а значит ванильная галочка автостроя к нему
применима, и игра сама тянет его к лимиту по своим правилам.

**Метод CM для этого не нужен и не годится**:
`cm_enable_auto_expand_for_building_type` не трогает ванильный флаг, он пишет
свои списки `cm_auto_expand_registered_building_types`, которые читает его
собственный круг стройки. Взять метод -- значит вернуть CM.

**Ванильная сторона, по файлам игры:** `IsAutoExpand(Building.Self)` и
`ToggleAutoExpandBuilding(Building.Self)` берут **Building**, не тип. Building
достаётся из `BuildingItem.GetBuilding` -- то есть из панели зданий самой игры,
а туда этот мод уже умеет ставить своё (`wtp_integration.md`).

**Одно решение за ним, и оно не техническое:** шаг «поставить первый уровень»
ставит здание **сразу и даром**. Это действие игры, а не планировщика, и
разрешить его может только он.

## Восьмой заход, 2026-09-14 — окно замены стало про здания, а не про товары

Он нашёл корень сам, описав симптомы: «нажал +1 на какой-то домик из ND…
добавилось сразу аж два домика», «нажал +1 на пиво лвл1, но добавилось пиво
лвл4», «список не соответствует доступности стране», «даже при выключенной
галочке справа всё так же отображаются все здания из ND», «кнопка "Отсеять" не
даёт вообще никакого эффекта».

**Одна причина на всё: окно показывало здания, а под кнопками был товар.**

* **Ворота кандидата** спрашивали `_edit_fits_<сторона>_<товар>` — «этот товар
  тут производится», — а не «это здание тут можно построить». Отсюда и чужие
  моды при снятой галочке, и четыре ступени одной лестницы, и ровно 24 (упор в
  `SWAP_MAX`). Теперь ворота свои: `_stands_<здание>` плюс `_avail_`/`_reach_`
  по тумблеру эпохи, а галочка мода живёт внутри `_avail_`.
* **«+» и «−»** звали эффекты редактора по **всем** товарам здания разом. Теперь
  вызов гейтится тем, что `_pm<товар>` принадлежит этому зданию, и `_swap_once`
  пропускает только первый: одно нажатие — один домик, и тот, по которому нажали.
* **«Отсеять»** сравнивал выгоду ТОВАРА, которая почти всегда больше нуля.
  Теперь у здания своё значение `_swap_gain_<здание>` — лучший из его товаров.
* **Имена печатались «...»**: тот же ноль ширины, что и у списка, этажом ниже.
* **`SWAP_MAX` 24 → 80.** Ровно 24 в каждой локации — это упор в потолок.

**Значение уехало в `script_values`** — сперва я выписал `_swap_gain_*` в файл
эффектов, тот же класс, что и триггер в файле эффектов.

**Поиск по грамоте теперь сортирует картой.** `_r_score` = `_r_fit` (это
`_rq<k>`, среднее покрытий связки) × 1000 + деньги на ничью.

## Девятый заход, 2026-09-14 — числа в окне замены и ступень лестницы

**Строка списка получила процент.** Достать число «по зданию из строки» этому
дереву нечем: `BuildingType.GetKey` в дампах нет, переменную на типе здания не
держит никто, `type = building_type` у кастомной локализации не встречается, а
переход в скоуп через `global_var:X = {{ }}` **внутри значения** не доказан
нигде. Доказан способ CMF: датамодель идёт по флагам, строка собирает имя
переменной через `Concatenate`. Значит места пронумерованы, а раскатана не
поиск, а **укладка** -- круг определён один раз, на место приходится вызов и три
строки.

**Кнопка «−» пропала по той же причине, что до неё имена.** Ряд должен быть
`hbox` с политикой **и** `size = {{ -1 h }}`, а дети -- со своими размерами. У
меня было то одно, то другое: твёрдое число на `hbox` не держится, а
растягивающийся ребёнок съедает соседей.

**Ступень лестницы -- только верхняя из открытых.** Игра сама знает, какая
устарела: `building_type_is_obsolete`, страновой триггер по типу здания; CM
спрашивает его той же формой.

**В строке поиска по грамоте появился процент пригодности** -- то же число, что
на карте. Сверять теперь есть чем.

## Десятый заход, 2026-09-14 — счётчик, ширина и честность «Пригодности»

**«Можно поставить (0)» при непустом списке** — счётчик читал
`_swap_list`, который я перестал заполнять, перейдя на `_swap_slots`. Одна
строка локализации, отставшая от кода.

**Кнопка «−» пропадала от ширины, а не от формы.** В файле она стояла всё это
время, один в один как рабочая «+». Сумма ячеек ряда была шире колонки, лишнее
срезается с правого края — то есть с кнопки. Ширину колонки из этого дерева не
вычислить (`window_margin_alt` в нём нет), поэтому не арифметика, а форма:
у ячейки имени теперь **и политика, и свой размер** — ужимается имя, кнопка
остаётся. Чек-лист эту пару и помечает как безопасную.

**Выгода в строке — доля, а не тысячные.** `_p<n>` из RANK_SCALE, 1000 и есть
100%; кладётся уже долей, печатается процентом.

**«Пригодность» — число карты, и оно слепо к эпохе.** Проверено по
`bag_wtp_trmm_values.txt`: у `_trmm_opt_*` нет ни одних ворот по продвижению,
CM берёт лучший способ рецепта безусловно. Значит это число конца игры **всегда**,
и он поймал это сам: «ты его спиздил на оба плана… а не считаешь по логике карты
самостоятельно». Поэтому:

* в подсказке столбца это сказано прямо, и туда же встал **разбор по товарам
  связки** — среднее из трёх покрытий роняет один товар, и какой именно, видно
  только разбором (его случай: 80.9% против 79.4% при большем сырье у второго);
* **порядок ведёт пригодность только в режиме «на конец»**; «на сейчас» сортирует
  деньгами, как раньше.

**Что осталось нечестным и ждёт решения:** сделать покрытие зависимым от эпохи
значит пересчитывать `opt` по доступности, то есть переписать расчёт CM, а не
скопировать его — и потерять сверку с картой один в один.

## Десятый прогон: что смотреть

1. **Окно замены слева**: «−» у каждой строки. Если её снова нет — значит дело
   не в ширине, и это надо сказать: форма теперь та, что чек-лист зовёт
   безопасной.
2. **«Можно поставить (N)»** — число, а не ноль.
3. **Процент справа** — трёхзначный максимум, 100.0% вместо 1000.
4. **Поиск по праву**: наведись на «Пригодность» — там разбор по товарам связки
   и прямая оговорка, что число не слушается тумблера эпохи.
5. В режиме **«на сейчас»** порядок снова по деньгам; пригодность ведёт порядок
   только «на конец».

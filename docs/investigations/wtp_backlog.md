# Список владельца после игры, 2026-09-12 — и что в нём измерено

**Это тот самый список, которого не было в документах**
([`NEXT_SESSION.md`](../NEXT_SESSION.md) просил его спросить). Он пришёл после
партии одним сообщением: девять пунктов плюс решение, которое их все окрашивает.
Здесь он записан целиком, каждый пункт — его словами, а под ним то, что **уже
измерено по коду**, и что для него надо построить.

**Ничего из этого в игре не проверено.** Пометка «измерено» значит «прочитано в
файлах игры и посчитано скриптом», не «видено в партии».

---

## Решение, из которого следует половина работы: CM и Glorp UI убраны

«Я решил полностью отказаться от CM и GlorpUI… Они оба снижают
производительность игры. Особенно сильно это делает CM. Поэтому переключатель
нашей автоматизации должен работать теперь не через CM, а через ванильную
систему автоматизации строительства.»

**CMF остаётся** — страница мода на нём, и он про него говорит отдельно
(«отдельную кнопку CMF»). Уходит `romaimperator.construction_manager`.

**Что мод берёт у CM сейчас** (измерено, `grep` по дереву мода): ровно две вещи.

| что | имя CM | где у нас |
| --- | --- | --- |
| галочки автостроя | `cm_auto_expand_registered_building_types`, `cm_auto_expand_excluded_building_types`, `cm_mass_auto_expand_building_types` | `bag_wtp_cm_apply` в `bag_wtp_generated_plan.txt` |
| пометка «житница» | `cm_auto_food_location_enabled` | `bag_wtp_is_granary` в `bag_wtp_generated_triggers.txt` |

**Ванильная замена автостроя — только из интерфейса, и это измерено.** В дампах
игры есть `IsAutoExpand( Arg0 )`, `ToggleAutoExpandBuilding( Arg0 )`,
`ToggleAutoExpandBuildings( Arg0 )`, `AreBuildingsAutoExpand( Arg0 )` и
`BuildInLocationLateralView.ToggleAutoExpandAllBuildings` — **всё это функции
GUI**. Ни эффекта, ни триггера с этим смыслом в дампах нет: `api.py --find
auto_expand` не находит ничего, кроме чужого `on_action` CM. Значит **отмашка
скриптом невозможна**, и наша кнопка обязана стать виджетом, который зовёт
`ToggleAutoExpandBuilding` на здании.

**И отсюда его единственная добавка**: «чтобы ещё не построенные здания всё так
же имели возможность включить для себя автоматизацию». Ванильная галочка висит
на `Building.Self` — на **существующем** здании; у непостроенного её нет.
Значит это наше: помнить намерение на локации и отдать его игре, когда здание
появится, либо строить первый уровень самим.

---

## Что осталось из первых девяти

Первые три захода закрыты и лежат в
[`../archive/wtp_backlog_third_pass.md`](../archive/wtp_backlog_third_pass.md)
— там же девять пунктов его первого прихода целиком. Открыто из них два:

- **9. Режим ручного заполнения.** «Выбрал землю, включил нужные тумблеры села,
  города, житницы. И заполняешь самостоятельно каждую локацию тем, что тебе
  нужно… Далее сделал, настроил, включил автостроительство.» Половина этого —
  окно замены, построенное 2026-09-13; не хватает тумблеров и отмашки.
- **Ванильная отмашка автостроя.** CM снят, а замена ей живёт только в
  интерфейсе: `ToggleAutoExpandBuilding` на существующем здании. Для
  непостроенного придётся помнить намерение на локации.

## Автострой: пункт закрыт его решением

**Его слово, 2026-09-14:** «я предпочту отсутствие вообще функции тумблера
стройки, нежели заставлю интегрировать её таким образом… Отъебись ради бога с
этой частью от меня, не предлагай.»

Пункт закрыт. Ни `allow` (запретил бы и ему), ни заказ стройки модом (он трижды
сказал: сами не строим), ни скрытый виджет-сторож. **Больше не предлагать.**

Почему подсунуть заглушку нельзя -- разобрано в
[`../RESEARCH.md`](../RESEARCH.md): `Building` и `Construction` -- объекты
движка, скрипт их не создаёт, а флаг авторасширения лежит полем на объекте, о
котором движок скрипт не спрашивает вовсе.

## Одиннадцатый и двенадцатый заходы — в архиве

«+» со своим эффектом, своё покрытие `_cov_pass` вместо числа карты, «−» на
здании, которое игра пускает и в село и в город, и полнота правого списка:
[`../archive/wtp_backlog_passes_11_12.md`](../archive/wtp_backlog_passes_11_12.md).
Из них живо одно — «Пригодность», и она в тринадцатом заходе ниже.

---

## Тринадцатый и четырнадцатый заходы — в архиве

Подсказка «Пригодности» рисовала разбор карты CM рядом с нашим столбцом; и сам
счёт оказался пересказом формулы CM, потерявшим вычитание уже выданной грамоты:
[`../archive/wtp_backlog_passes_13_14.md`](../archive/wtp_backlog_passes_13_14.md).

## Пятнадцатый заход, 2026-09-14 — эпоха, и кто её спрашивает

Он открыл режим специализации и увидел: по подсказке лучшая грамота на земле —
текстильная (100 %), а раздача поставила судматериалы. И: «вообще там в целом не
должно быть этой мусорной строки +0%».

**Три вещи, и первая объясняет его вопрос целиком.**

**1. Подсказка строки плана тоже была чужой.** В поиске я её переписал, а в окне
плана осталась лесенка CM (`_trmm_ur_rank_slot_*`). У CM ворот по эпохе нет
вовсе — он берёт лучший вариант безусловно, — а раздача выбирает грамоту нашим
`_rq<k>`, который эпохе подчиняется. Поэтому подсказка и говорила «текстиль
100 %» (по-игровому это «на конец»), а раздача считала на сейчас и ставила
судматериалы. Его догадка про «сейчас/конец» была верной, только расхождение
было не внутри расчёта, а между расчётом и подписью под ним.

Теперь подсказка строки плана — наша: та же лесенка `_rqr<k>`, тот же разбор по
товарам, и строкой ниже сказано, что лесенка про локацию, а грамота выдаётся на
провинцию по среднему её городов.

**2. Проход покрытия читал тумблер чужого окна.** `_cov_pass` всегда гейтился на
`_plan_by_end`, а звали его двое: план (его тумблер — этот) и поиск по грамоте
(его тумблер — `_rank_by_end`). Значит галочка «Считать на конец игры» **в
поиске** для столбца «Пригодность» не делала ничего. Теперь возраст ставит
вызывающий: `_cov_age_plan` / `_cov_age_rank` пишут одну служебную глобалку
`_cov_end`, и проход читает её.

**3. Эпоха подписана там, где от неё зависит число** — в подсказке «Пригодности»,
в подсказке строки плана, в шапке правого списка окна замены и в двух столбцах
грамот в сводке. Подпись берётся из тумблера **того** окна, о котором идёт речь,
поэтому её две (`_age_plan`, `_age_rank`), в двух областях видимости каждая.

**И мусорная строка убрана.** `+0 %` от местного модификатора больше не
печатается: строка товара стала `customizable_localization` и выбирает короткий
вид, когда раскладывать нечего.

**Чекер.** `foreign_breakdown_in_own_text` в `tools/check_script.py` больше не
даст нарисовать разбор перенесённой карты в нашем тексте — проверено на подсадке.

## Шестнадцатый заход, 2026-09-14 — прогон с логами

Первый его прогон, к которому приложены логи, и они нашли три ошибки, невидимые
на экране. Запись целиком — [`../TESTLOG.md`](../TESTLOG.md).

**Что он принял:** «Эйфель набрал 79.4 % а Эмсланд 76.1 % и в целом теперь всё в
нужном порядке и сходится». Вычитание уже выданной грамоты и было причиной.

**Что чинено здесь.**

**1. «На конец» не значило «когда-нибудь».** `_reach_<n>` выписан только у 13
методов из 241, что за недостижимым продвижением; у остальных он не нужен,
потому что они достижимы по построению. А проход покрытия, не найдя `_reach_`,
подставлял `_avail_` — «может построить **сейчас**». Значит «на конец»
совпадало с «на сейчас» почти везде, и лесенка не двигалась от галочки вовсе:
«права в расчёте всегда показываются одни и те же». Теперь там, где `_reach_`
нет, ответ `always = yes`. То же исправлено в списке окна замены.

**2. Модификатор вывода выброшен целиком.** «Наш мод интересует только то, что
даёт провинция по РГО бонусу, а тот модификатор какой-то костыль для той карты,
он нам не нужен.» У CM он есть; у нас теперь нет, и это второе (и последнее)
наше отличие от карты после отбора способов по доступности.

**3. Лесенка показывает только то, что этой державе светит.** Константинопольская
монополия ушла (`has_or_had_tag = BYZ`), фламандская осталась (культурная
группа) — тем же триггером `_plan_right_gate_<k>`, которым раздача и решает.
Ноль в лесенку тоже не попадает. Места считаются только среди показанных, иначе
выброшенные оставляли бы дыры.

**4. Подробности — три лучших права, а не одно**, и каждое со своим именем.

**5. Специализация: лесенка и раздача считают одним числом.** Грамота выдаётся
провинции по среднему её городов; лесенка показывала счёт одной локации.
Теперь в этом режиме раздача кладёт своё среднее в `_rqv<k>` и пересчитывает
места — то, что он видит в подсказке, и есть то, чем выбрана грамота.

**6. `ERROR 1/2`** — `Method.key` склеен из частей через `+`, а
`ShowProductionMethodName` ждёт одно определение (`error.log`, 3 077 строк).
Части печатаются поимённо, ссылкой `$ключ$`; проверено, что ключ есть у всех
530 частей.

**7. Из `debug.log`:** проход покрытия звался из плана без
`scope:bag_wtp_country` (1 448 строк), `_r_fit_pct` читал переменную там, где
прохода не было (240), и разбор `potential` чужого продвижения съедал соседний
блок целиком, отчего в воротах оказался `ai_weight` (16).

**Чекер.** `unresolved_localization_refs` в `tools/check_script.py`: `$ключ$` без
ключа и `Custom('имя')` без `customizable_localization` — за один день я
перепутал их дважды.

## Прогон: что смотреть

1. **Режим специализации: лесенка и выданная грамота обязаны совпасть первой
   строкой.** Это одно и то же число теперь — среднее по городам провинции.
   Если разошлись, скажи обе цифры: значит `_plan_right_fits_<k>` отсекает
   грамоту, у которой счёт выше, и это будет следующая правка.
2. **Галочка «на конец» меняет лесенку.** Раньше числа стояли те же, и это было
   главной причиной всего остального. Переключи и сравни хотя бы две строки.
3. **В лесенке нет константинопольской монополии и нет нулей**, а фламандское
   сукно есть.
4. **Под лесенкой три права, а не одно**, каждое со своим именем.
5. **`ERROR` в разборе способа пропал**, на его месте имя способа. Если рецепт
   собран из двух, их два через запятую.
6. **Подпись эпохи** следует галочке своего окна: план — своей, поиск — своей.

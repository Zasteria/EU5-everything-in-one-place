# Read-order file: the engine picks the FIRST valid levy across all levy
# files in name order, so the prefix encodes this file's unlock age exactly
# like vanilla (00_revolutions ... 05_traditions). The extra 0 after the
# prefix sorts us before the vanilla file of the same age.

# Requisitionnaires (age 6): the permanent requisition took the young men of
# every class, so peasants, laborers and burghers all serve.
levy_nd_fra_requisitionnaires = {
	size = levy_generic_infantry_size
	unit = a_nd_fra_requisitionnaires

	allowed_pop_type = peasants
	allowed_pop_type = laborers
	allowed_pop_type = burghers

	country_allow = {
		has_or_had_tag = FRA
	}
}
